# Use the Windows system trust store so Gradle can reach Maven Central over HTTPS.
# Fixes: java.security.InvalidAlgorithmParameterException: the trustAnchors parameter must be non-empty
org.gradle.jvmargs=-Djavax.net.ssl.trustStore=NUL -Djavax.net.ssl.trustStoreType=Windows-ROOT
