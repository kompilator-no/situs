# Always checkout shell scripts with LF so they are executable on Linux
gradlew        text eol=lf
*.sh           text eol=lf

# Windows scripts can keep CRLF
*.bat          text eol=crlf
*.cmd          text eol=crlf

# Everything else — let Git decide
*              text=auto
